import { connect } from './accounts';
import { update, signOn, reload, signUp, verified, createFailed } from './members';

export default [ connect, update, signOn, reload, verified, signUp, createFailed ];